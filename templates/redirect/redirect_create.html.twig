{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Create Redirect' %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('arrow-up-right-square') ~ ' Redirects', 'redirect_index'),
    bootstrap_breadcrumb('Create'),
  ) }}
{% endblock %}
