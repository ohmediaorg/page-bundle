{{ meta_entity(meta) }}

{% if page.noindex %}
<meta name="robots" content="noindex" />
{% endif %}

{% set canonical_url = page_path(canonical_path) %}
<link rel="canonical" href="{{ absolute_url(canonical_url) }}" />

<script type="application/ld+json">{{ breadcrumb_schema|js }}</script>
